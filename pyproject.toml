[project]
name = "ai-multi-agent-loop"
version = "0.1.0"
description = "Excersise application from Building with Claude API course"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=1.8.0",
    "python-dotenv>=1.2.3",
]
